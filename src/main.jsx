import React from "react";
import ReactDOM from "react-dom/client";
import Emberhold from "./Emberhold.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Emberhold />
  </React.StrictMode>
);
